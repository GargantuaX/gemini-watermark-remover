# 1.0.42 发布前质量复核（2026-09-09）

当前仍不满足发布条件。历史报告的 `clean` 标签不能替代目视验收，也不能通过更新基线数字消除已经看到的退化。

## 复核范围与结果

- 原始样本集为 424 张；本次盲评覆盖 61 张位置变化样本及 16 张历史恢复样本的并集，共 69 张。
- 历史实现为 `d4717dc`（1.0.30）；已核对其源文件与历史发布证据中的哈希一致。
- 对照实现为合入 #153 后的 `07b059a8`。逐张核验输入哈希，使用原始分辨率处理，保存原图及两个版本输出的同位置裁剪。
- A/B 顺序打乱，先记录目视判断，再揭示版本：当前更好 34 张、历史更好 13 张、接近 19 张、尚不明确 3 张。
- 这不是 424 张的完整视觉验收。接近不代表干净，算法的质量标签也不是独立验证依据。

本地复核材料位于 `.artifacts/release-review-20260909/`：`manifest.json`、`review-key.json`、`blind-decisions.json`、`unblinded-review.json`、`diagnostics.json` 及裁剪图。输入文件与样本编号的对应关系以该 manifest 为准。

## 已定位并修正的错位分支

`clean-skip-04` 和 `clean-skip-05` 是两张 1024×1024 图片：

- 真正的白色水印在 `(880, 880)`，尺寸 48×48，右下边距各 96px。
- 自动选择器将 `(929, 929)` 的背景细节识别成 36px V2 水印；固定选择器没有选中候选。
- 这个 V2 候选使流程提前确认水印存在，因此跳过独立的 exact-48 源图证据补救。结果保留原水印，同时在背景上制造深色三角形。
- 两张原图在真实锚点上的空间相关性约 0.185/0.195，梯度相关性约 0.548/0.542，均具有正向白色证据和明显的局部边缘峰值。

修复只在固定选择器未选中、自动选择器选中 36px V2 时补做已有 exact-48 验证；仅当原有带符号源图及诱饵位置门槛通过、且两者几何位置不兼容时，转入既有 0.6 增益的补救流程。没有放宽证据门槛、移除极性保护或增加修补算法。

两张真实样本的右下 480×480 裁剪已加入测试夹具。回归测试在修复前选错位置而失败，修复后通过；它还检查背景误识别区域逐像素不变，以及真实水印核心确实得到处理。另有空白/暗色旧锚点的 V2 保留控制。

离线重跑同一批 69 张，只有这两张输出变化，其余 67 张逐像素一致。目视检查显示背景误伤消失、真实水印明显减弱，但中心仍有少量残留，不能标记为完全清除。

## 其余发现

| 复核编号 | 已观察到的差异 | 后续需要的证据 |
| --- | --- | --- |
| clean-skip-07 | 位置相同，旧输出边缘残留更少 | 比较 alpha 边缘及清理阶段，不能直接恢复旧清理策略 |
| clean-skip-09 | 真实水印未去除，当前标准位置补救新增黑洞 | 在真实水印位置重新推导几何和极性；旧输出也未正确清除 |
| position-22、30、31、33、47、49、62 | 尺寸/位置或 alpha 模型变化，产生更厚轮廓或暗色残留 | 固定源图几何与跨模板观察器，区分定位误差、模板形状和强度误差 |
| position-41 | 选中不相交的前景区域，留下原水印并新增星形痕迹 | 对裁剪图片推导锚点，验证真实水印与内容诱饵的区别 |
| position-56 | 当前跳过，历史输出减少了可见白色水印 | 核查裁剪/缩放后的预览锚点及存在性证据 |
| clean-skip-03、position-18、position-26 | 两种结果各有残留或内容变化，优劣不明确 | 原尺寸局部及完整画面进一步复核 |

发布仍需处理上述问题并重新生成与最终源代码一致的质量证据。#153 的修复和单元测试通过不代表这些历史退化已经解决；也不代表网站已升级其独立的 SDK Worker。
