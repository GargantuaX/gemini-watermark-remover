name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.11.0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate sample fixtures
        run: |
          if [ ! -d src/assets/samples ] || [ -z "$(ls -A src/assets/samples)" ]; then
            echo "Missing regression fixtures in src/assets/samples"
            exit 1
          fi

      - name: Build
        run: pnpm build

      - name: Project surface regression
        run: node --test tests/project/frontendI18nRetirement.test.js tests/project/publicStaticSurfaceRetirement.test.js tests/project/deploymentRetirement.test.js

      - name: SDK publish smoke
        run: pnpm test:sdk-smoke

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Test
        run: pnpm test

      - name: Package release candidate tarball
        run: |
          mkdir -p .artifacts/release-candidate
          pnpm pack --pack-destination .artifacts/release-candidate/

      - name: Upload release candidate artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-candidate-artifacts
          path: |
            .artifacts/release-candidate/*.tgz
            dist/
          if-no-files-found: error
          retention-days: 7
